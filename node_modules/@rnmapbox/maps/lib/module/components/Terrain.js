import React, { memo, useMemo } from 'react';
import { requireNativeComponent } from 'react-native';
import { transformStyle } from '../utils/StyleValue';
export const NATIVE_MODULE_NAME = 'RCTMGLTerrain';
export const Terrain = /*#__PURE__*/memo(props => {
  let {
    style = {}
  } = props;
  if (props.exaggeration) {
    console.warn(`Terrain: exaggeration property is deprecated pls use style.exaggeration instead!`);
    style = {
      exaggeration: props.exaggeration,
      ...style
    };
  }
  const baseProps = useMemo(() => {
    return {
      ...props,
      reactStyle: transformStyle(style),
      style: undefined
    };
  }, [props, style]);
  return /*#__PURE__*/React.createElement(RCTMGLTerrain, baseProps);
});
const RCTMGLTerrain = requireNativeComponent(NATIVE_MODULE_NAME);
//# sourceMappingURL=Terrain.js.map